
% Jan 9, 2020

% 'pwd' will show the working directory

% replace backslashes with forward ones when directory is listed:
% ['C:', strrep(pwd, '/', '\')]

% 'ls' will print out the sub-files in the current main folder

% 'whos' provides info on files size, bytes, etc.

% 'cd' can change the working directory

% cd('..') changes directory the the parent of current folder

% 'mkdir' and 'rmdir' make and remove a directory

% use 'save' and 'load' to save and load stuff in the command window

% save variables 's' and 'a' into a data file called my_data_file
% save my_data_file s a